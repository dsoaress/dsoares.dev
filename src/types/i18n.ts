export type I18n = {
  resume: {
    title: string
    file: string
  }
  projects: {
    title: string
    description: string
  }
  errors: {
    notFound: string
    internalError: string
    buttonLabel: string
  }
  footer: string
}
