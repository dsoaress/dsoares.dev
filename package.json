{
  "name": "dsoares.me",
  "author": "Daniel Soares <daniel.soares@me.com>",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "lerna run --scope site dev",
    "dev:og-image": "lerna run --scope og-image dev",
    "build": "lerna run --scope site build",
    "start": "lerna run --scope site start",
    "postinstall": "lerna bootstrap",
    "analyze": "lerna run --scope site analyze",
    "clean": "lerna run clean",
    "format": "prettier --write \"./**/*.{js,jsx,ts,tsx,json}\""
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.3.0"
  }
}
