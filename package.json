{
  "private": true,
  "license": "MIT",
  "scripts": {
    "backend": "yarn workspace backend develop",
    "frontend": "yarn workspace frontend develop",
    "start": "concurrently --kill-others-on-fail \"yarn backend\"  \"yarn frontend\"",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md,yml}\""
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^5.3.0",
    "prettier": "^2.1.2"
  }
}

